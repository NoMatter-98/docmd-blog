# ---------------------------------------------------
# GitHub Pages Deployment Workflow
# Generated by docmd v0.9.0
# Project: 商子羽的博客
# https://docmd.io/deployment/github-pages
# ---------------------------------------------------

name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install dependencies
        run: npm install

      - name: Patch slugify for CJK support
        run: node scripts/patch-slugify.js

      - name: Build site
        run: npx docmd build

      - name: Add CNAME for custom domain
        run: echo "wujiangang.com" > ./site/CNAME

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
