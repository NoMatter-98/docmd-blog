1、远程无非两种方法，一是公网IP端口映射，显然目前不行。另一种是用公网IP服务器做握手，即内网穿透。
2、远程使用的主要功能是：远程操作（可图形化界面）、传输多个小文件
有可能有整合这俩功能的，纯开源底层协议（原始）的方法是openssh+ssh的文件传输+VNC协议这两招，然后要加内网穿透。不过老实说实在是有点慢，《交响情人梦》截图这种稍微大一点到100MB的就要等很久。

手机到电脑-像安卓的X-plore就有ssh连接后传输文件的功能，非常方便。